# C题问题3：三时间尺度因果滚动随机MPC

## 1. 任务与实际实现概览

本项目已实现“0点日前计划—6/12/18点购电调整—每10分钟储能反馈”的三时间尺度控制器。程序每天0:00生成144个10分钟时段的全天初始计划 `g0`；被策略启用的6:00、12:00、18:00节点使用最新预测重算，但每次只正式锁定到下一个启用更新节点前的交付区间；每个10分钟时段的真实负荷和光伏到达后，再由因果DP执行器决定真实充放电、紧急购电和弃光/无法利用电量。

实现方法是“两阶段补救近似 + 因果滚动随机MPC”，不是严格多阶段场景树，也不声称得到严格多阶段全局最优解。0:00的共享 `g0` 确实把未来允许调整的时段和对应费用纳入优化；情景相关补救变量提供未来调整机会的近似价值。该补救近似仍比严格非预见场景树乐观，这是当前模型的主要限制之一。

## 2. 与问题2的区别

问题2的整日随机计划和历史整轨迹抽样思路可以复用，但问题3增加了三个关键层次：

- `g0` 是0:00提交的全天初始购电计划，所有调整费用始终相对当天 `g0` 结算；
- 6:00、12:00、18:00的远期解只是 `proposed_g`，只有进入本次至下一更新之间的交付块才成为 `committed_g`；
- 情景中的储能变量只用于规划，真实动作不复制情景解，而是在真实值到达后由10分钟反馈器生成。

问题2代码中的数据读取、全日残差轨迹和储能状态转移思路经过时序审查后被重新实现；没有修改 `solve_problem2.py`、其副本或任何附件。

## 3. 只读项目审计结果

编码前实际检查到的核心文件如下：

- `附件/附件1.xlsx`：`Sheet1`，144×4数据；字段为时间、电价、小区负载、光伏发电预测功率；首末时刻为00:10和`0:00+1`。
- `附件/附件2.xlsx`：工作表 `小区负载`、`光伏发电实际功率`；两表均为365×145数据，日期为2025-01-01至2025-12-31，144个时段从00:10至`0:00+1`。
- `附件/附件3.xlsx`：`Sheet1`，1460×26数据；字段为日期、预报时刻、预报1小时至预报24小时；每天4条0/6/12/18点预测。日期列有1095个向下省略单元格，程序先 `ffill`。
- `附件/附件5/result3.xlsx`：4个工作表，分别为 `计划购电量`、`调整购电量`、`充放电量`、`紧急购电量`。前两表为335×147，覆盖2月1日至12月31日。
- 问题2可复用结构：宽表读取、固定电价读取、连续历史残差轨迹抽样、HiGHS线性规划、SOC网格DP、守恒检查和提交表结构。

发现一个模板与强制口径的冲突：模板前两表第一个时段标题为`0:10-0:20`，最后一个为`0:00-0:10+1`，整体比附件1/2和“00:10表示(00:00,00:10]”的口径晚一格。程序不修改官方模板标题，而把第1至第144个数值按列序映射到真实slot 0至143；内部CSV和账本使用无歧义区间，如 `(00:00, 00:10]`。该冲突也记录在 `run_config.json`。

## 4. 时间索引与单位

内部统一用区间结束时刻表示交付时段。slot 0的目标时刻为00:10，对应 `(00:00,00:10]`；slot 35的目标时刻为06:00；6:00更新只能影响slot 36，即从 `(06:00,06:10]` 开始。12:00和18:00同理。

附件中的功率全部先转换成每10分钟电量：

\[
E_t=P_t\times\frac{10}{60}=\frac{P_t}{6}\quad(\mathrm{kWh}).
\]

电价保持元/kWh，因此电量乘电价得到元。

## 5. 信息边界与walk-forward场景

规划函数只接收预测场景、当前SOC、固定价格、策略和当天 `g0`，不接收尚未发生的当天真实负荷或真实光伏。当天真实数组只在规划求解完成后切出当前交付块，传入执行器逐时结算。

负荷基准预测使用决策日期以前最多30天的同槽均值；历史不足时使用附件1曲线冷启动。6/12/18点可以使用当天已经结束时段的负荷误差中位数做指数衰减偏差修正，不能使用当前决策后的真实值。

场景不是对144个时段独立采样。程序按相同发布时间从历史记录中建立完整24小时的负荷与光伏联合残差轨迹，再整轨迹抽取并叠加到当前基准预测，因此保留时间相关性以及负荷/光伏之间的同期关系。候选轨迹必须同时满足：

\[
\text{source issue}<\text{decision time},\qquad
\text{source issue}+24\mathrm{h}\leq\text{decision time}.
\]

第二个条件确保历史轨迹的最后目标时刻也已经成熟。窗口随时间向前滚动，仅使用决策日期/时刻以前的数据。历史为空时退化为单条基准场景，并在审计表中记录场景数为1。

## 6. 光伏插值

附件3每条记录给出未来24个整点功率。决策时刻已观测的实际光伏作为0小时锚点，`预报1小时`至`预报24小时`作为1至24小时锚点，用线性插值生成每10分钟功率，再除以6得到电量。跨日目标用绝对时间索引处理。2025-01-01 0:00没有前一交付区间，按午夜光伏为0初始化锚点。

## 7. 规划变量与约束

规划LP包含共享初始计划或当前提交块购电、情景相关未来购电、充电、放电、SOC、紧急购电、多余电量，以及相对 `g0` 的向上/向下调整量。每个情景满足：

\[
g_t+d_t+e_t=L_t-V_t+c_t+w_t,
\]

\[
S_{t+1}=S_t+0.9c_t-\frac{d_t}{0.9},
\]

\[
1200\le S_t\le10800,quad
0\le c_t,d_t\le833.333333.
\]

不允许售电，所有购电和动作变量非负。LP先使用连续变量，并加入 `epsilon=1e-4` 元/kWh的吞吐惩罚。程序自动报告 `min(charge,discharge)` 的最大值；实际全年基线和单日8组合验证均为0，因此没有升级为MILP。

## 8. 目标函数与结算

当天0:00计划费用为 `p_t g0_t`。最终购电量相对同一 `g0` 只结算一次：

\[
a^-_t=\max(g^0_t-g_t,0),\qquad
a^+_t=\max(g_t-g^0_t,0).
\]

向下调整退回额以非负数保存，汇总时做减法。统一总费用为：

\[
C=\sum_t\left[p_tg^0_t-0.5p_ta^-_t+1.5p_ta^+_t
+5p_te_t+\epsilon(c_t+d_t)\right].
\]

CSV同时保存 `base_purchase_cost_yuan`、`downward_refund_yuan`、`upward_premium_yuan`、`emergency_cost_yuan`、`throughput_penalty_yuan`、结算费用和总费用。没有把最终购电再按普通电价重复收费。

## 9. 0/6/12/18点滚动流程

每个策略每天按下列顺序运行：

1. 0:00：读取当前SOC和0:00预测，求共享全天 `g0`。LP允许未来被启用更新覆盖的时段使用情景补救调整，因此 `g0` 考虑未来调整机会及费用。
2. 交付到下一更新前：购电量固定；每10分钟真实值到达后执行一次储能反馈并结算。
3. 6/12/18点（若策略启用）：历史时段不会进入优化；用当前真实SOC和新预测求解。只把到下一启用更新前的共同解写入 `committed_g`，更远部分只记为 `proposed_g`。
4. 所有调整仍相对0:00的 `g0` 结算，因此一个交付时段不会被连续节点重复收费。

`decision_ledger.csv`逐次记录 `decision_time`、`target_time`、`forecast_issue_time`、`g0_kwh`、`proposed_g_kwh`、`committed_g_kwh`、上次承诺和本次是否锁定。0:00的初始合同是全天 `g0`；日内节点只修改其本次锁定块。

## 10. 真实执行与紧急购电

规划情景动作不用于真实执行。对每个提交块，程序先基于整段相关场景反向计算SOC网格价值函数；当前10分钟的真实净负荷到达后，选择满足功率/SOC限制且“当前紧急购电费用 + 吞吐惩罚 + 后续价值”最小的状态转移。由于一次状态转移只能向上或向下，真实动作结构上不会同时充放电。

实际平衡为：

\[
g_t+V_t+d_t+e_t=L_t+c_t+w_t.
\]

`w_t` 在程序中命名为 `curtailment_or_unused_kwh`：它是不能被负荷或储能吸收的总多余电量，主要对应弃光，但在已经承诺购电且实际负荷偏低时也可能包含无法利用的合同电量，所以不能在所有时段都严格解释成纯光伏弃光。

## 11. 跨日SOC与终端价值

1月1日0:00从6000 kWh开始，之后每个日期直接继承前一天24:00的SOC。1月既用于在线模型初始化，也逐10分钟真实运行；没有在2月1日重置。

每次6/12/18点预测窗覆盖未来24小时，因此18:00优化显式包含次日信息。非年末LP对24小时末SOC赋予 `0.9×预测窗电价中位数` 的剩余价值，执行DP跟踪规划情景的块末平均SOC，从而避免在午夜前无意义放空。只有2025-12-31 24:00使用所有策略共同的硬终端条件 `SOC=6000 kWh`。没有强制每日SOC回到日初值。

## 12. 消融与Shapley设计

代码实现全部 \(2^3=8\) 种更新组合：`000=S0`、`100=S06`、`010=S12`、`001=S18`、`110=S0612`、`101=S0618`、`011=S1218`、`111=S061218`。每个策略从相同初始SOC独立运行，并使用相同场景数、历史窗和种子。命令行也兼容题目中的别名 `S6=S06`、`Sall=S061218`。`--ablation`运行顺序边际所需的4种累计策略，`--all-subsets`运行全部8种并生成Shapley值。

时刻 \(i\) 的Shapley成本节约为：

\[
\phi_i=\sum_{A\subseteq N\setminus\{i\}}
\frac{|A|!(3-|A|-1)!}{3!}\,[C(A)-C(A\cup\{i\})].
\]

正值表示加入该预测时刻降低总费用；负值表示在给定近似策略和样本中反而增费。最终判断依据是含紧急购电和吞吐项的实际总费用，不以预测误差替代。

## 13. 程序结构

- `read_problem_data`：读取附件、字段/形状/日期审计、单位转换。
- `interpolate_pv_forecast`：24个整点预测插值为10分钟预测。
- `forecast_load`：walk-forward历史负荷曲线和已实现前缀偏差修正。
- `build_scenarios`：筛选成熟历史误差并抽取联合连续轨迹。
- `solve_stochastic_mpc`：构造并求解两阶段补救LP，检查HiGHS状态。
- `feedback_value_function`、`execute_feedback_block`：规划块价值函数和因果真实执行。
- `simulate_strategy`：跨日SOC连续的计划、调整、执行、结算和账本。
- `validate_run`：完整性、时序、守恒、边界、费用和信息成熟性检查。
- `compute_shapley`：由8组合实际成本计算三时刻贡献。
- `write_result3`：复制模板并写入2月1日至12月31日结果。

## 14. 运行命令

本机环境实际使用Anaconda Python 3.13，依赖为NumPy、pandas、SciPy、openpyxl，无新增安装。常用命令：

```powershell
# 1月1日单策略验证
python solve_problem3.py --data-dir ./附件 --out-dir ./analysis_outputs/problem3_smoke --date 2025-01-01

# 非1月1日未给initial-soc时，自动从1月1日因果回放到目标日
python solve_problem3.py --date 2025-02-01 --out-dir ./analysis_outputs/problem3_replay

# 显式SOC的孤立单日诊断（输出会标记diagnostic模式）
python solve_problem3.py --date 2025-06-20 --initial-soc 7000 --out-dir ./analysis_outputs/diagnostic

# 正式全年基线并复制、填充模板
python solve_problem3.py --all-year --strategy S061218 --out-dir ./analysis_outputs/problem3

# 全部8种组合及Shapley；策略规模较大，建议单独目录
python solve_problem3.py --all-year --all-subsets --out-dir ./analysis_outputs/problem3_ablation
```

命令行同时接受 `--output-dir` 和题目示例中的 `--out-dir`。路径均通过 `pathlib.Path` 处理，无机器绝对路径硬编码。

## 15. 已完成的实际运行结果

### 正式全年基线

正式输出使用 `S061218`、8条场景、30天历史窗、随机种子2025、100 kWh SOC网格。实际回放365天、52,560个时段，耗时217.65秒。结果为：

| 指标 | 实际值 |
|---|---:|
| 总费用 | 19,147,419.88 元 |
| 初始计划基准费用 | 15,158,365.45 元 |
| 调整净费用 | 2,399,531.08 元 |
| 紧急购电费用 | 1,588,251.12 元 |
| 紧急购电量 | 433,185.37 kWh |
| 向上调整量 | 2,269,133.09 kWh |
| 向下调整量 | 2,085,123.34 kWh |
| 弃光/无法利用电量 | 3,898,913.10 kWh |
| 储能吞吐量 | 12,722,377.24 kWh |

1月31日末SOC与2月1日初SOC在独立跨月验证中均为7800 kWh，证明2月没有重置。正式全年最后时刻SOC为6000 kWh，满足统一终端条件。

### 8组合单日端到端验证

在2025-01-01、相同初始SOC=6000、随机种子2025、4场景上实际运行了全部8种组合。该日历史尚为空，各策略实际使用同一条冷启动场景；因此这些数字用于验证流程和Shapley代码，不代表全年预测时刻价值。总费用依次为：S0 125,167.12、S06 110,696.48、S12 107,581.29、S18 111,320.09、S0612 97,832.08、S0618 99,856.87、S1218 107,771.37、S061218 98,022.16元。对应Shapley成本节约为：6点11,608.69元、12点9,209.03元、18点6,327.24元。

### 全年8组合消融与边际价值

全年8组合采用相同的4场景、200 kWh网格、30天历史窗、随机种子2025和初始SOC=6000；每种策略均独立从1月1日回放，并共享同一终端条件。它是为完成组合比较而运行的较轻配置，不能把其中 `S061218` 的21,592,644.00元与上面的8场景/100 kWh正式基线19,147,419.88元直接作策略差异解释。

| 组合 | 总费用/元 | 调整净费用/元 | 紧急购电费用/元 | 紧急购电量/kWh | 弃光或无法利用/kWh | 储能吞吐/kWh |
|---|---:|---:|---:|---:|---:|---:|
| 000 / S0 | 22,470,796.70 | 0.00 | 5,070,274.87 | 1,125,976.55 | 7,360,407.22 | 11,164,583.68 |
| 100 / S06 | 23,099,786.37 | 2,410,938.19 | 5,498,609.14 | 1,194,216.95 | 5,894,405.02 | 11,682,958.05 |
| 010 / S12 | 22,006,466.51 | 1,504,364.10 | 4,839,371.19 | 1,071,366.15 | 5,392,584.42 | 12,127,392.46 |
| 001 / S18 | 22,066,449.84 | 2,072,260.80 | 3,739,648.78 | 883,958.15 | 6,265,421.69 | 11,234,678.46 |
| 110 / S0612 | 21,983,246.08 | 2,035,449.10 | 4,755,623.44 | 1,064,676.39 | 4,877,479.16 | 12,207,759.20 |
| 101 / S0618 | 22,081,655.13 | 3,374,918.59 | 3,537,777.48 | 843,923.77 | 5,442,843.64 | 11,662,366.44 |
| 011 / S1218 | 21,622,970.99 | 2,417,535.11 | 3,562,126.81 | 838,044.43 | 5,207,023.14 | 11,930,034.99 |
| 111 / S061218 | 21,592,644.00 | 2,940,278.83 | 3,488,403.24 | 833,401.92 | 4,682,788.67 | 11,934,641.77 |

按累计顺序 `000→100→110→111` 计算，6点、12点、18点的条件边际成本节约分别为-628,989.67元、1,116,540.29元、390,602.08元。负值表示只加入6点更新在该近似和参数下增加了调整/紧急购电综合成本，并非程序把“价值”强制为正。使用全部8个子集计算的Shapley成本节约为：6点 **-198,218.37元**、12点 **577,783.63元**、18点 **498,587.44元**；三者合计878,152.70元，等于 `C(S0)-C(S061218)`。在这组实际回放中，12点预测的平均合作贡献最大，18点次之，6点贡献为负，因此不能笼统断言每增加一次更新都会降低总费用。

完整数值（还包括初始计划费用、上下调整量和运行时间）保存在 `analysis_outputs/problem3/strategy_comparison_8_subsets_full_year.csv`，Shapley结果保存在 `shapley_values_full_year.csv`。

## 16. 自动检查结果

正式全年基线的 `checks.json` 全部通过：

- 365天、每天144时段，52,560条结算记录，无重复日期-slot；
- 所有数值有限，非负量最小值为0；
- 最大电量平衡残差 `3.1841e-13 kWh`；
- 最大SOC递推残差 `1.1369e-13 kWh`；
- 最大充电量833.333333 kWh，最大放电量833.141514 kWh；
- 实际同时充放电时段数0，规划LP最大同时充放电量0；
- 成本恒等式最大残差 `9.095e-13` 元（CSV往返后的浮点舍入量级）；
- 全部历史误差均在决策时刻前成熟；
- 跨日SOC连续残差0，年末终端SOC残差0。

工作簿验证结果：4个要求工作表均存在；计划和调整表均为335×147；334个日期行和144个时段值没有空白；指定日期2025-03-20、2025-06-20、2025-09-20、2025-12-20分别可在第49、141、233、324行提取。`充放电量`有2004条4小时块记录，`紧急购电量`按正紧急购电时段展开。

## 17. 输出文件

- 根目录 `result3.xlsx`：正式全年基线提交表。
- `analysis_outputs/problem3/problem3_detail.csv`：完整时段执行、SOC与费用。
- `analysis_outputs/problem3/daily_summary.csv`：逐日汇总。
- `analysis_outputs/problem3/decision_ledger.csv`：计划、提案和承诺账本。
- `analysis_outputs/problem3/forecast_audit.csv`：情景来源、成熟性、求解状态和规划同时充放电审计。
- `analysis_outputs/problem3/checks.json`、`run_config.json`：自动检查和可复现参数。

## 18. 假设、近似与未完成限制

- 0:00 LP是共享初始计划加情景相关补救的两阶段近似；情景补救知道整条抽样轨迹，没有构造严格多阶段非预见场景树，因此可能低估未来调整成本。
- 负荷预测采用透明、可复现的历史均值和短期偏差修正，没有训练更复杂模型。
- 历史情景从最多30天、同一发布时间的完整24小时成熟轨迹抽取；早期样本较少，1月1日只能使用确定性冷启动。
- `curtailment_or_unused_kwh` 是总多余电量，并非在所有时段都能严格拆成纯弃光与弃购电；若竞赛口径要求纯弃光，需要增加购电不可弃置约束或进一步分解变量。
- SOC反馈使用离散网格。正式基线步长100 kWh，全8组合全年消融步长200 kWh，二者不能直接作同精度成本排名。
- CVaR未纳入正式基线。题目要求的实际总购电费用始终是主目标；代码当前也未实现CVaR敏感性，不能报告期望成本、CVaR和风险权重曲线。
- 没有启用MILP，因为吞吐惩罚后规划与实际执行均未发现明显同时充放电。
- 官方模板时段标题错位按列序兼容，内部时序和所有结算严格采用结束时刻口径。

## 19. 后续改进

下一步可构建带聚类/削减的严格多阶段场景树，并在6/12/18节点施加逐层非预见约束；改进概率负荷预测；将多余电量拆成纯弃光和合同电弃置；对场景数、历史窗、SOC网格和终端价值做系统敏感性；最后在保留实际总费用主指标的同时增加可选CVaR，并同时报告无CVaR基准、期望成本、CVaR、高分位成本和风险权重 `lambda`。
